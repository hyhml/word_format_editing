# 通用公文格式示例

这是当前项目的第一个格式包，用于验证模块 0 的格式包复用机制。

## 来源

- `source/format_spec.example.json`

## 用途

- 验证单文件 hash。
- 验证多文件组合 hash 的顺序无关设计。
- 验证 `formats/` 注册表扫描。
- 验证精确匹配和元数据匹配。

## 格式概要

- 页面：A4，纵向。
- 正文：宋体/Times New Roman，12pt，1.5 倍行距，两端对齐。
- 标题：通过段落位置和正则表达式识别。
- 表格：五号左右的表格文字，居中，表头可加粗。
